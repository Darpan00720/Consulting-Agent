---
title: M1.7.8 Design — M1.7 Closure
status: PROPOSED
date: 2026-07-04
inputs: [M1.7-Design §D6 scheduled-debt table, Architecture-Checkpoint-M1 TD register,
         pyproject.toml, .python-version, docs/implementation/M1-Decomposition.md,
         CHANGELOG.md, docs/implementation/BACKLOG.md, all M1.7.x design docs]
evidence-discipline: every statement classified [Verified] / [Inference] / [Unknown]
---

# M1.7.8 Design — M1.7 Closure

# Purpose

Close M1.7 (Concurrency, Versioning & Corrections): discharge the remaining
scheduled chores (TD-008, TD-009, the TD-003 BACKLOG note), reconcile the
documentation that drifted during the eight-sub-milestone build, and produce
the **M1.7 Definition-of-Done completion report**. **Docs, tooling config, and
hygiene only** — no runtime, behaviour, architecture, API, validation, replay,
or append change.

# Repository Evidence

M1.7-Design §D6 scheduled five debts into M1.7 [Verified — L296–301]: TD-002,
TD-004 (**done, M1.7.5**), TD-010 (**done, M1.7.7**), and **TD-008, TD-009
(still open)**. Plus D6 §L542 promised a TD-003 BACKLOG note that was never
written. Evidence per item:

- **TD-008 — tooling vs runtime version** [Verified]: `.python-version` = 3.12;
  `pyproject.toml` pins ruff `target-version="py311"` (L31), black
  `target-version=["py311"]` (L51), mypy `python_version="3.11"` (L54),
  `requires-python=">=3.11"` (L5). The linters/type-checker target an older
  language level than the only version the project pins and tests on.
- **TD-009 — decomposition doc inconsistency** [Verified]: `M1-Decomposition.md`
  §M1.7 (L108–111) describes the component as
  `packages/state/_concurrency.py (private) + R/W-matrix data;
  tests/state/test_concurrency.py`. **None of that was built.** The actual M1.7
  implementation is the `packages/state/append/` package (errors, result,
  sequencing, versioning, guard, commit, pipeline, integrity) plus
  `state/ownership.py`, delivered across sub-milestones **M1.7.1–M1.7.8** — a
  structure the doc does not mention.
- **TD-003 — approval-actor rules** [Verified]: consistently referenced as
  *deferred to M6* across seven docs (M1.7-Design, M1.7.5/6 designs,
  traceability, two reviews). But `BACKLOG.md` tracks **only TD-001** — the
  M1.7-Design promise that the deferral is "recorded in BACKLOG" (L542) is
  unfulfilled.
- **CHANGELOG** [Verified]: entries for M1.7.1 (L155), M1.7.2 (L146), all
  M1.7.3 slices, M1.7.4, M1.7.5, M1.7.6, M1.7.7 are present and complete. The
  section header still reads "**— in progress**".
- **Design-doc statuses** [Verified]: M1.7-Design, M1.7.4/5/6/7 all **APPROVED**;
  `M1.7.3-Decomposition.md` still **PROPOSED** although M1.7.3 shipped (S1–S6
  done; S7/S8 dispositioned — S8 folded into M1.7.7, S7 deferred to M1.9).
- **API / performance / ownership / replay / traceability docs** [Verified
  complete]: `EngagementState.md` + `Events.md` document the append API and the
  seq contract (M1.7.3-S6); `docs/performance/baselines.md` is the consolidated
  authority (M1.7.7); `traceability-ADR-002.md`/`.json` carry rules +
  dispositions + ownership (M1.7.5/6); replay integrity is documented in its
  design + module contract. Nothing missing.
- **No M1.7 completion / DoD report exists** [Verified: absence] — the headline
  deliverable of this milestone.

# Remaining Technical Debt

| TD | State | Evidence |
|---|---|---|
| TD-001 | Open (unchanged; M0/M1.1 backlog) | BACKLOG.md |
| TD-002 | **Closed** — LIFE-005..008 | M1.7.5 |
| TD-003 | Deferred → M6 (approval-actor roles) | 7 docs; **needs BACKLOG note** |
| TD-004 | **Closed** — disposition table | M1.7.5 |
| TD-005 | **Closed** — snapshot semantics (D1) | M1.7.1 |
| TD-006 | **Closed** — seq/ordering contract (D2) | M1.7.3/M1.7.4 |
| TD-007 | Deferred → M1.8 (event upcasting) | M1.7.4 §R16 |
| TD-008 | **Open — this milestone** | pyproject vs .python-version |
| TD-009 | **Open — this milestone** | M1-Decomposition §M1.7 |
| TD-010 | **Closed** — validation baseline | M1.7.7 |
| TD-011 | Deferred (duplicated analysis-field maps) | checkpoint register |

# Documentation Audit

| Doc | State | Action |
|---|---|---|
| CHANGELOG M1.7.1–7 entries | complete | header "in progress" → mark M1.7 complete on closure |
| M1.7.x design docs | APPROVED except… | flip `M1.7.3-Decomposition.md` → COMPLETE + record S7/S8 disposition |
| M1-Decomposition §M1.7 | **stale (TD-009)** | rewrite to the as-built structure + sub-milestone list |
| API docs (EngagementState/Events) | complete | none |
| performance/baselines.md | complete | (optional) cross-link from CHANGELOG/README |
| traceability, ownership, replay | complete | none |
| M1.7 completion report | **missing** | create `docs/reviews/M1.7-Completion-Report.md` |

# Tooling Audit

`.python-version=3.12` is the single pinned/tested runtime [Verified]. The four
pins targeting 3.11 are the inconsistency. **Proposed resolution (DD-A):** align
everything to 3.12 — ruff `target-version="py312"`, black `target-version=
["py312"]`, mypy `python_version="3.12"`, `requires-python=">=3.12"` — dropping
the *untested* 3.11 support claim. **Risk (flagged):** bumping ruff/black
target-version can change lint suggestions / formatting; bumping mypy's
python_version can surface or resolve type differences. The implementation must
re-run the full gate and land any resulting formatting/lint deltas in the same
chore commit (this is config, not runtime behaviour). If the gate produces
non-trivial churn, that is reported, not worked around.

# Consistency Audit

- `M1.7.3-Decomposition.md` status vs reality: **PROPOSED but shipped** → fix.
- `M1-Decomposition.md` §M1.7 vs reality: **wrong component/test names** → fix
  (TD-009).
- BACKLOG vs TD register: BACKLOG lists only TD-001; the checkpoint holds the
  full register. **Proposed (DD-C):** make BACKLOG the live register — add the
  TD-003 deferral note (fulfilling the D6 promise) and a compact "closed in
  M1.7" list (TD-002/004/005/006/010) with milestone refs, plus the standing
  deferrals (TD-007→M1.8, TD-011). No new debt invented; this only reflects
  what the checkpoint already records.
- CHANGELOG header "in progress" vs M1.7 being closed by this milestone → mark
  complete (DD-D).

# Closure Tasks

1. **TD-008:** align the four version pins to 3.12; re-run the gate; land any
   formatting/lint delta in the same commit.
2. **TD-009:** rewrite `M1-Decomposition.md` §M1.7 to the as-built structure
   (append/ package + ownership.py; sub-milestones M1.7.1–8) — a doc fix only.
3. **Doc statuses:** `M1.7.3-Decomposition.md` → COMPLETE (record S7 deferred to
   M1.9, S8 folded into M1.7.7); confirm all other M1.7 design docs APPROVED.
4. **BACKLOG:** add TD-003 note + reconcile the TD register (DD-C).
5. **CHANGELOG:** mark the M1.7 section complete (DD-D); optional baseline
   cross-link.
6. **M1.7 completion report:** `docs/reviews/M1.7-Completion-Report.md` — the
   DoD roll-up across M1.7.1–8 (deliverables, invariants, deferrals, gate
   state, what M1.8 inherits).

# Risks

- **Low — TD-008 churn:** the only item that can touch tracked non-doc files
  (config + any reformat). Mitigated by re-running the full gate and reporting
  deltas; still zero *runtime* change.
- **Low — over-reach:** the temptation to "improve" prose beyond corrections.
  Scope is strictly corrections/reconciliation; no rewrites of accurate docs.
- **None architectural:** no source, no behaviour, no ADR, no public API.

# Definition of Done

- **Files:** `pyproject.toml` (4 pins); `docs/implementation/M1-Decomposition.md`
  (§M1.7); `docs/implementation/M1.7.3-Decomposition.md` (status);
  `docs/implementation/BACKLOG.md` (TD register); `CHANGELOG.md` (M1.7 complete);
  new `docs/reviews/M1.7-Completion-Report.md`; this doc's status.
- **Tests:** none new (no code). The **full six-step gate must stay green** —
  it is the proof that the TD-008 config bump changed nothing that matters
  (ruff/black/mypy/pytest all pass on 3.12 targets; 267 tests unchanged).
- **Documentation:** all audits above resolved; completion report delivered.
- **Acceptance criteria:** gate green; `packages/` zero-diff (no source touched);
  TD-008/TD-009 closed; TD-003 recorded in BACKLOG; M1.7.3-Decomposition +
  CHANGELOG statuses consistent; completion report present.
- **Technical-debt disposition:** TD-008/TD-009 closed here; TD-003→M6,
  TD-007→M1.8, TD-011 deferred and recorded; TD-001 remains open (out of M1.7
  scope).
- **Risks / architectural concerns:** only the Low items above.

# Decisions requiring approval

- **DD-A — TD-008 direction:** bump all four pins to **3.12** and
  `requires-python>=3.12` (drop untested 3.11 support). Alternative: keep
  `>=3.11` floor and leave target-versions at py311 (documents "we support 3.11"
  — but we never test it). **Recommend 3.12 everywhere.**
- **DD-B — S7 (property/stress) final disposition:** confirm S7 stays **out of
  M1.7**, scheduled to M1.9 (as recorded in M1.7.7). M1.7.8 only records the
  disposition; it does not implement S7.
- **DD-C — BACKLOG reconciliation extent:** make BACKLOG the live TD register
  (add TD-003 + closed/deferred summary), vs leaving it minimal and pointing to
  the checkpoint. **Recommend reconcile.**
- **DD-D — CHANGELOG closure:** mark the M1.7 section complete (drop "in
  progress") as the last M1.7.8 commit.
- **DD-E — Completion-report location:** `docs/reviews/M1.7-Completion-Report.md`
  (alongside the checkpoint and readiness reviews). Confirm.
