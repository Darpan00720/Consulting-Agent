# StratAgent environment configuration — copy to .env (which is git-ignored).
# All variables are prefixed STRATAGENT_ and read by core.config.Settings.

STRATAGENT_ENVIRONMENT=development
STRATAGENT_LOG_LEVEL=INFO

# LLM / tool backend credentials (used in later milestones; NOT required for M0):
# ANTHROPIC_API_KEY=
